---
import Base from '../layouts/Base.astro';
---

<Base title="Homepage di esempio" description="Esempio di homepage con Astro e Tailwind">
  <section class="py-10">
    <div class="container mx-auto">
      <h1 class="text-4xl font-extrabold tracking-tight">Benvenuto 👋</h1>
      <p class="mt-3 text-slate-600 max-w-prose">
        Questa è una homepage di esempio. Usa Astro per il rendering statico e Tailwind per lo stile.
      </p>
      <div class="mt-6 flex gap-3">
        <a href={import.meta.env.BASE_URL} class="px-4 py-2 rounded bg-sky-600 text-white hover:bg-sky-700">Vai alla Home</a>
        <a href={`${import.meta.env.BASE_URL}posts/`} class="px-4 py-2 rounded border border-sky-600 text-sky-700 hover:bg-sky-50">Vai al Blog</a>
      </div>
    </div>
  </section>

  <section class="py-10 bg-slate-50">
    <div class="container mx-auto grid md:grid-cols-3 gap-6">
      <div class="p-6 bg-white rounded border border-slate-200">
        <h2 class="font-semibold">Astro</h2>
        <p class="text-sm text-slate-600">Siti statici veloci, DX moderna.</p>
      </div>
      <div class="p-6 bg-white rounded border border-slate-200">
        <h2 class="font-semibold">Markdown</h2>
        <p class="text-sm text-slate-600">Scrivi contenuti nel formato che ami.</p>
      </div>
      <div class="p-6 bg-white rounded border border-slate-200">
        <h2 class="font-semibold">Tailwind</h2>
        <p class="text-sm text-slate-600">Utility-first per prototipare rapidamente.</p>
      </div>
    </div>
  </section>
</Base>

